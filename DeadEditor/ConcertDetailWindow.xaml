<Window x:Class="DeadEditor.ConcertDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Concert" Height="750" Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Concert Content -->
        <Grid Grid.Row="0" Margin="30,30,30,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Album Artwork and Info -->
            <StackPanel Grid.Column="0" Margin="0,0,30,0">
                <!-- Artwork with placeholder -->
                <Border Background="#2D2D30" CornerRadius="8" Margin="0,0,0,20">
                    <Grid Width="280" Height="280">
                        <!-- Placeholder -->
                        <Grid x:Name="ArtworkPlaceholder">
                            <Grid.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#3E3E42" Offset="0"/>
                                    <GradientStop Color="#2D2D30" Offset="1"/>
                                </LinearGradientBrush>
                            </Grid.Background>
                            <Viewbox Width="120" Height="120">
                                <TextBlock Text="🎵" Foreground="#787878" FontSize="80"/>
                            </Viewbox>
                        </Grid>
                        <!-- Actual artwork -->
                        <Image x:Name="AlbumArtwork"
                               Stretch="UniformToFill"/>
                    </Grid>
                </Border>

                <!-- Concert Info -->
                <TextBlock x:Name="VenueText"
                           FontWeight="SemiBold"
                           FontSize="20"
                           TextWrapping="Wrap"
                           Foreground="#FFFFFF"
                           Margin="0,0,0,8"/>
                <TextBlock x:Name="LocationText"
                           FontSize="15"
                           Foreground="#9CDCFE"
                           TextWrapping="Wrap"
                           Margin="0,0,0,4"/>
                <TextBlock x:Name="DateText"
                           FontSize="15"
                           Foreground="#B5CEA8"
                           Margin="0,0,0,15"/>
                <TextBlock x:Name="TrackCountText"
                           FontSize="13"
                           Foreground="#808080"/>
            </StackPanel>

            <!-- Right: Track List -->
            <Border Grid.Column="1"
                    Background="#252526"
                    CornerRadius="8"
                    BorderBrush="#3F3F46"
                    BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Border Grid.Row="0"
                            Background="#2D2D30"
                            BorderBrush="#3F3F46"
                            BorderThickness="0,0,0,1"
                            Padding="15,12">
                        <TextBlock Text="TRACKS"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="#CCCCCC"/>
                    </Border>

                    <!-- Track List -->
                    <DataGrid Grid.Row="1"
                              x:Name="TracksDataGrid"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              SelectionMode="Single"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="None"
                              Background="#252526"
                              RowBackground="#252526"
                              AlternatingRowBackground="#2D2D30"
                              Foreground="#CCCCCC"
                              BorderThickness="0"
                              Margin="5"
                              LoadingRow="TracksDataGrid_LoadingRow">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#252526"/>
                                <Setter Property="Foreground" Value="#808080"/>
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#3F3F46"/>
                            </Style>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#37373D"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#094771"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#CCCCCC"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Foreground" Value="#FFFFFF"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="#" Binding="{Binding TrackNumber}" Width="50"/>
                            <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                            <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" Width="90"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <!-- Audio Player Controls -->
        <Border Grid.Row="1"
                Background="#2D2D30"
                BorderBrush="#3F3F46"
                BorderThickness="0,1,0,0"
                Padding="20,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Now Playing Info -->
                <StackPanel Grid.Row="0" Margin="0,0,0,12">
                    <TextBlock x:Name="NowPlayingText"
                               Text="No track playing"
                               FontWeight="SemiBold"
                               FontSize="13"
                               Foreground="#FFFFFF"/>
                    <TextBlock x:Name="NowPlayingDetails"
                               Text=""
                               FontSize="11"
                               Foreground="#808080"
                               Margin="0,2,0,0"/>
                </StackPanel>

                <!-- Playback Controls -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Transport Buttons -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,20,0">
                        <Button x:Name="PreviousButton" Content="⏮" Width="50" Height="38" Margin="0,0,6,0"
                                Click="PreviousButton_Click" IsEnabled="False"
                                Background="#3E3E42" Foreground="#CCCCCC" BorderBrush="#555555" FontSize="16"/>
                        <Button x:Name="PlayPauseButton" Content="▶" Width="60" Height="38" Margin="0,0,6,0"
                                Click="PlayPauseButton_Click" FontWeight="Bold" IsEnabled="False"
                                Background="#0E639C" Foreground="#FFFFFF" BorderBrush="#1177BB" FontSize="18"/>
                        <Button x:Name="StopButton" Content="⏹" Width="50" Height="38" Margin="0,0,6,0"
                                Click="StopButton_Click" IsEnabled="False"
                                Background="#3E3E42" Foreground="#CCCCCC" BorderBrush="#555555" FontSize="16"/>
                        <Button x:Name="NextButton" Content="⏭" Width="50" Height="38"
                                Click="NextButton_Click" IsEnabled="False"
                                Background="#3E3E42" Foreground="#CCCCCC" BorderBrush="#555555" FontSize="16"/>
                    </StackPanel>

                    <!-- Scrubber -->
                    <Grid Grid.Column="1" Margin="20,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock x:Name="CurrentTimeText" Grid.Column="0"
                                   Text="0:00" VerticalAlignment="Center"
                                   Margin="0,0,12,0" Foreground="#CCCCCC" FontSize="12"/>
                        <Slider x:Name="AudioScrubber" Grid.Column="1"
                                Minimum="0" Maximum="100"
                                VerticalAlignment="Center"
                                ValueChanged="AudioScrubber_ValueChanged"
                                PreviewMouseDown="AudioScrubber_PreviewMouseDown"
                                PreviewMouseUp="AudioScrubber_PreviewMouseUp"
                                Foreground="#0E639C" Background="#3E3E42"/>
                        <TextBlock x:Name="TotalTimeText" Grid.Column="2"
                                   Text="0:00" VerticalAlignment="Center"
                                   Margin="12,0,0,0" Foreground="#CCCCCC" FontSize="12"/>
                    </Grid>

                    <!-- Volume Control -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="20,0,0,0">
                        <TextBlock Text="🔊" VerticalAlignment="Center"
                                   Margin="0,0,10,0" FontSize="16"/>
                        <Slider x:Name="VolumeSlider" Width="120"
                                Minimum="0" Maximum="100" Value="75"
                                VerticalAlignment="Center"
                                ValueChanged="VolumeSlider_ValueChanged"
                                Foreground="#0E639C" Background="#3E3E42"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
