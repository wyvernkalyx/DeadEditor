<Window x:Class="DeadEditor.MatchToSongDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Match to Song" Height="420" Width="500"
        Background="#1E1E1E"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#0E639C"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1177BB"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#3C3C3C"/>
                    <Setter Property="Foreground" Value="#666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Match track to setlist song"
                   FontSize="20" FontWeight="Bold"
                   Foreground="#FFFFFF"
                   Margin="0,0,0,12"/>

        <!-- Current track title -->
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Current title:" Foreground="#999999" FontSize="13" Margin="0,0,0,4"/>
            <TextBlock x:Name="CurrentTitleText"
                       Foreground="#D7BA7D" FontSize="15" FontWeight="SemiBold"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Unmatched setlist songs -->
        <Border Grid.Row="2" Background="#252526" BorderBrush="#3E3E42" BorderThickness="1" CornerRadius="3">
            <ListBox x:Name="SetlistSongsListBox"
                     Background="Transparent"
                     Foreground="#E0E0E0"
                     FontSize="14"
                     BorderThickness="0"
                     SelectionChanged="SetlistSongsListBox_SelectionChanged">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="8,6"/>
                        <Setter Property="Foreground" Value="#E0E0E0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#094771"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2A2D2E"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Cancel" Width="100" Margin="0,0,10,0"
                    Background="#3C3C3C" Click="CancelButton_Click"/>
            <Button x:Name="MatchButton" Content="Match" Width="100"
                    IsEnabled="False" Click="MatchButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
