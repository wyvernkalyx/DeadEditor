<Window x:Class="DeadEditor.ReleaseSelectorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Release" Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E"
        Foreground="White"
        ResizeMode="CanResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Multiple releases found. Please select one:"
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Album Info -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock x:Name="AlbumInfoTextBlock" TextWrapping="Wrap" FontSize="14" Foreground="#CCCCCC"/>
        </StackPanel>

        <!-- Releases List -->
        <Border Grid.Row="2" BorderBrush="#555555" BorderThickness="1" Background="#2D2D30">
            <DataGrid x:Name="ReleasesDataGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      SelectionMode="Single"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      Background="#2D2D30"
                      Foreground="White"
                      RowBackground="#2D2D30"
                      AlternatingRowBackground="#252526"
                      BorderThickness="0"
                      MouseDoubleClick="ReleasesDataGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Artist" Binding="{Binding Artist}" Width="150" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Album Title" Binding="{Binding Title}" Width="*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Year" Binding="{Binding Year}" Width="80" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Label" Binding="{Binding Label}" Width="140" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Country" Binding="{Binding Country}" Width="90" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Format" Binding="{Binding Format}" Width="90" IsReadOnly="True"/>
                </DataGrid.Columns>
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#3E3E42"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="8,5"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        <Setter Property="BorderBrush" Value="#555555"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="8,5"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#094771"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
            </DataGrid>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Select" Width="100" Height="35" Margin="0,0,10,0" Click="SelectButton_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="100" Height="35" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
