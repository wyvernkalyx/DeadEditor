<UserControl x:Class="DeadEditor.ConcertDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#1E1E1E"
             Loaded="ConcertDetailView_Loaded">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Concert Metadata -->
        <StackPanel Grid.Column="0">
            <TextBlock x:Name="DateText" Text=""
                       Foreground="#E0E0E0" FontSize="22" FontWeight="Bold"
                       Margin="0,0,0,12"/>

            <TextBlock x:Name="VenueText" Text=""
                       Foreground="#CCCCCC" FontSize="15"
                       Margin="0,0,0,6" TextWrapping="Wrap"/>

            <TextBlock x:Name="LocationText" Text=""
                       Foreground="#E0E0E0" FontSize="15"
                       Margin="0,0,0,12" TextWrapping="Wrap"/>

            <TextBlock x:Name="SongCountText" Text="0 songs"
                       Foreground="#888888" FontSize="14"
                       Margin="0,0,0,4"/>

            <TextBlock x:Name="HeadyCountText"
                       Foreground="#D4A017" FontSize="13"
                       Margin="0,0,0,0"
                       Visibility="Collapsed"/>

            <!-- External Links -->
            <TextBlock x:Name="SetlistFmLink"
                       Text="setlist.fm ↗"
                       Foreground="#5B9BD5" FontSize="13"
                       Margin="0,4,0,0"
                       Cursor="Hand"
                       Visibility="Collapsed"
                       MouseLeftButtonUp="SetlistFmLink_Click">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="TextDecorations" Value="Underline"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- Library Recordings / Import section (populated in code-behind) -->
            <StackPanel x:Name="LibrarySection" Margin="0,16,0,0"/>
        </StackPanel>

        <!-- Right: Setlist -->
        <Grid Grid.Column="1" Margin="20,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="SETLIST"
                       Foreground="#E0E0E0" FontSize="16" FontWeight="SemiBold"
                       Margin="0,0,0,14"/>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="SetlistPanel"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
