<UserControl x:Class="DeadEditor.BoxSetsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#1E1E1E">

    <UserControl.Resources>
        <!-- Styles cloned verbatim from ConcertDatabaseView.xaml:11-55 so the two
             list views look identical. -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#2A2D3E"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="MinHeight" Value="40"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2A2D3A"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#37373D"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- DataGrid and empty-state TextBlock overlay one another in the same Grid;
         LoadBoxSets toggles visibility based on item count. -->
    <Grid>
        <DataGrid x:Name="BoxSetsDataGrid"
                  AutoGenerateColumns="False"
                  SelectionMode="Single"
                  IsReadOnly="True"
                  Background="#252526"
                  RowBackground="#252526"
                  AlternatingRowBackground="#2D2D30"
                  Foreground="#CCCCCC"
                  GridLinesVisibility="None"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="False"
                  CanUserSortColumns="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  EnableRowVirtualization="True">

            <DataGrid.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#37373D"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#E0E0E0"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#2D2D30"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="#E0E0E0"/>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <!-- Verified glyph column. Binary (verified vs not) — no tri-state Partial
                     needed since a BoxSetDefinition is single-file, not merged. Reuses
                     BadgeVerifiedGlyph (#4EAA3F) from App.xaml. Matches the dimensions
                     from the Library-grid polish (commit c014c97): font 18, width 36,
                     centered. -->
                <DataGridTemplateColumn Header="" Width="36" MinWidth="36" MaxWidth="36" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="18"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource BadgeVerifiedGlyph}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value=""/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Verified}" Value="True">
                                                <Setter Property="Text" Value="&#x2713;"/>
                                                <Setter Property="ToolTip" Value="Verified"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" MinWidth="240"/>
                <DataGridTextColumn Header="Release Date" Binding="{Binding ReleaseDate}" Width="Auto" MinWidth="120"/>
                <DataGridTextColumn Header="Label" Binding="{Binding Label}" Width="Auto" MinWidth="120"/>
                <DataGridTextColumn Header="Discs" Binding="{Binding DiscCount}" Width="Auto" MinWidth="65"/>
                <DataGridTextColumn Header="Concerts" Binding="{Binding Concerts.Count}" Width="Auto" MinWidth="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Empty state — visible only when no definitions exist. -->
        <TextBlock x:Name="EmptyStateText"
                   Text="No box sets yet. Click '+ New Box Set' in the header to create one."
                   Foreground="#888888"
                   FontSize="16"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="Collapsed"/>
    </Grid>
</UserControl>
