<UserControl x:Class="DeadEditor.LibraryGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#1E1E1E"
             Loaded="LibraryGridView_Loaded">

    <Grid>
        <!-- Loading indicator (shown while library is scanning) -->
        <TextBlock x:Name="LoadingIndicator"
                   Text="Loading library..."
                   Foreground="#888888"
                   FontSize="18"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="Collapsed"/>

        <DataGrid x:Name="ShowsDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="None"
                  Background="#252526"
                  RowBackground="#252526"
                  AlternatingRowBackground="#2D2D30"
                  Foreground="#CCCCCC"
                  BorderThickness="0"
                  Margin="20"
                  MouseDoubleClick="ShowsDataGrid_MouseDoubleClick"
                  CellEditEnding="ShowsDataGrid_CellEditEnding">

            <DataGrid.Resources>
                <!-- Override WPF system selection colors -->
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#37373D"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#E0E0E0"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#2D2D30"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="#E0E0E0"/>

                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#2A2D3E"/>
                    <Setter Property="Foreground" Value="#E0E0E0"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="FontSize" Value="15"/>
                    <Setter Property="Padding" Value="12,10"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#3E3E42"/>
                    <Setter Property="MinHeight" Value="40"/>
                </Style>

                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#E0E0E0"/>
                    <Setter Property="MinHeight" Value="40"/>
                    <Setter Property="FontSize" Value="16"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#2A2D3A"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#37373D"/>
                            <Setter Property="Foreground" Value="#FFFFFF"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>

                <Style TargetType="DataGridCell">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="12,8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="Auto" MinWidth="100"/>
                <DataGridTextColumn Header="Album Name" Binding="{Binding AlbumName}" Width="Auto" MinWidth="150"/>
                <DataGridTextColumn Header="Venue" Binding="{Binding Venue}" Width="Auto" MinWidth="120"/>
                <DataGridTextColumn Header="City, State" Binding="{Binding Location}" Width="Auto" MinWidth="120"/>
                <DataGridTextColumn Header="Tracks" Binding="{Binding TrackCount}" Width="Auto" MinWidth="60"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
