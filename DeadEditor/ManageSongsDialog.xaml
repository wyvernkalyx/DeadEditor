<Window x:Class="DeadEditor.ManageSongsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Song Database" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="400" MinWidth="600">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Song Database Browser" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Statistics -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock x:Name="StatsTextBlock" Text="" Foreground="Gray" FontSize="12"/>
        </StackPanel>

        <!-- Search and Filter -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Search:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" x:Name="SearchTextBox" Height="28"
                     TextChanged="SearchTextBox_TextChanged"
                     ToolTip="Search song titles and aliases"/>

            <TextBlock Grid.Column="2" Text="Artist:" VerticalAlignment="Center" Margin="15,0,10,0"/>
            <ComboBox Grid.Column="3" x:Name="ArtistFilterComboBox" Width="200" Height="28"
                      SelectionChanged="ArtistFilterComboBox_SelectionChanged"/>
        </Grid>

        <!-- Song List -->
        <Border Grid.Row="3" BorderBrush="#CCCCCC" BorderThickness="1" Background="White">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="SongsItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" Padding="10,8">
                                <StackPanel>
                                    <!-- Song Title and Artist -->
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Title}"
                                                   FontWeight="SemiBold" FontSize="13"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Artist}"
                                                   Foreground="#666666" FontSize="11"
                                                   VerticalAlignment="Center" Margin="10,0,0,0"/>
                                    </Grid>

                                    <!-- Aliases (if any) -->
                                    <TextBlock Text="{Binding AliasesDisplay}"
                                               Foreground="#888888" FontSize="11"
                                               Margin="0,3,0,0" TextWrapping="Wrap"
                                               Visibility="{Binding HasAliases}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Bottom Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <TextBlock x:Name="ResultCountTextBlock" Text=""
                       Foreground="Gray" FontSize="12"
                       VerticalAlignment="Center" Margin="0,0,15,0"/>
            <Button x:Name="ExportButton" Content="Export to Text" Width="120" Height="32"
                    Margin="0,0,10,0" Click="ExportButton_Click"
                    ToolTip="Export song list to a text file"/>
            <Button x:Name="CloseButton" Content="Close" Width="100" Height="32"
                    Click="CloseButton_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
